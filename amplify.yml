frontend:
  phases:
    build:
      commands:
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '*/'
  cache:
    paths:
      - node_modules/*/
      - .next/cache/*/